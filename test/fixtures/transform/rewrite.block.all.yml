input: |
  {
    // #rewrite-enable
    // #if(os="mac"|"windows")
    // "key": "#{os}"
    // #else
    // "key": "linux"
    // #endif

    // #if(os="mac"|"windows")
    // "key2": "#{os}"
    // #else
    // "key2": "linux"
    // #endif
  }
output: |
  {
    // #rewrite-enable
    // #if(os="mac"|"windows")
    "key": "mac"
    // #else
    // "key": "linux"
    // #endif

    // #if(os="mac"|"windows")
    "key2": "mac"
    // #else
    // "key2": "linux"
    // #endif
  }
args:
  os: mac
